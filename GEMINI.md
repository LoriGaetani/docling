Sei un esperto di python e flask. 

Crea uno script python `initial.sh` per creare l'env di python.

Crea un file `requirements.txt` con tutti i reqs di questa app.

Crea un `app.py` che legga il file `file.pdf` e usi docling per fare il parsing del file in ingresso.

Salva l'output in un file json.

Importante usare docling.